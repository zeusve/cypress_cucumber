pipeline {
    agent any
    triggers {
        githubPush()
    }
    stages {
        stage('Build') {
            steps {
                // Tus comandos de construcción
            }
        }
        stage('Test') {
            steps {
                // Tus comandos de prueba
            }
        }
        // Otros stages si los tienes
    }
}
